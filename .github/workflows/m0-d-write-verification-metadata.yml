name: M0-D Write Verification Metadata

on:
  pull_request:
    branches: [ main ]
    paths:
      - "gradle/libs.versions.toml"
      - "gradle/verification-metadata.xml"
      - "app/build.gradle.kts"
      - "app/src/benchmark/**"
      - "benchmark/**"
      - ".github/workflows/m0-d-write-verification-metadata.yml"

permissions:
  contents: write

concurrency:
  group: m0-d-verification-metadata-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  write-metadata:
    if: github.head_ref == 'test/7-measurement-harness'
    runs-on: ubuntu-24.04
    timeout-minutes: 20

    steps:
      - name: Checkout M0-D branch
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          ref: test/7-measurement-harness
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 # v5.2.0
        with:
          distribution: temurin
          java-version: "17"

      - name: Resolve benchmark dependencies and write SHA-256 metadata
        shell: bash
        run: |
          set +e
          ./gradlew --write-verification-metadata sha256             check             assembleDebug             :benchmark:assembleBenchmark
          status=$?
          set -e

          if git diff --quiet -- gradle/verification-metadata.xml; then
            echo "No dependency verification metadata changes were produced."
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add gradle/verification-metadata.xml
            git commit -m "build(benchmark): update dependency verification metadata"
            git push origin HEAD:test/7-measurement-harness
          fi

          echo "Initial Gradle status=$status; Bootstrap Verify owns the final compile/test verdict."
